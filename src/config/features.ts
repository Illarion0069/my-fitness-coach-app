// Feature flags — toggle to roll out features publicly.
// When SHOP_PUBLIC is false, the "Shop" section is only visible to trainers (admins).
// Flip to true to make it visible to everyone (including logged-out visitors).
export const SHOP_PUBLIC = false;
